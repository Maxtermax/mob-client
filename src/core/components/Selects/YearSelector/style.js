import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => ({
  formControl: {
    display: "flex",
    placeContent: "center",
    width: "100%",
    maxWidth: 500,
    marginLeft: "auto",
    marginRight: "auto",
    marginTop: 10,
    marginBottom: 25
  }
}));
